# macOS 系统垃圾文件
.DS_Store
._.DS_Store
*.DS_Store

# Terraform 本地缓存与状态文件（绝对不能提交）
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfstate.backup
**/.terraform.lock.hcl
.terraformrc
terraform.rc

# Terraform 敏感变量文件（含密码、token、私钥等）
*.tfvars
*.tfvars.json
*.auto.tfvars
*.auto.tfvars.json

# Terraform 本地覆盖文件（用于调试）
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# 通用开发工具临时文件
crash.log
crash.*.log
**/.decrypted~*

# Go 语言编译产物
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work

# 项目自定义忽略
hacks/**
*.xpkg

# Kubernetes & Talos 客户端配置文件（含真实凭证）
kubeconfig
talosconfig
.kube/config
.kubeconfig

# 明文密钥文件（安全红线，绝对禁止提交）
*.agekey
*.key
*.pem
*.crt
*.cer
*.pub
*.gpg
*.pgp
*.asc

# SOPS/VSCode 临时解密文件（防止泄密）
**/*.decrypted
**/*.decrypted~*